Map the DOCX OpenXML to this Agility content item.

The content item context below contains the current content item data and should be treated as the available content model context. Use its field names exactly.

## Content item context

```json
{{CONTENT_ITEM_JSON}}
```

## DOCX OpenXML

```xml
{{DOCX_XML}}
```
